<!DOCTYPE html>
<html lang="en">
	<head>
  	<%= render "common/head" %>
	</head>
	<body>
  		<div class="overlay hidden"></div>
    	<%= yield :nav_bar %>
    	<%= yield %>
		<%= yield :body %>
		<%= render "common/footer" %>
	</body>
</html>

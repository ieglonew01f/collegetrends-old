<%= form_for(post_like) do |f| %>
  <% if post_like.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(post_like.errors.count, "error") %> prohibited this post_like from being saved:</h2>

      <ul>
      <% post_like.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :post_id %>
    <%= f.number_field :post_id %>
  </div>

  <div class="field">
    <%= f.label :liked_by %>
    <%= f.number_field :liked_by %>
  </div>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
